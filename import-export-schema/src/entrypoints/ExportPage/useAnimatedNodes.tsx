import type { AppNode } from '@/utils/graph/types';

/**
 * Placeholder hook that can later introduce entrance/exit animations for nodes.
 */
export function useAnimatedNodes(initialNodes: AppNode[]) {
  return initialNodes;
}
